from model import make_model
import numpy as np

speed = np.loadtxt("train.txt")

x = 

model = make_model()

model.fit(x=None,
          y=None,
          batch_size=None,
          epochs=1,
          verbose=1)
